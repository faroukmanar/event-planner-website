/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",  // Adjust paths for all JS and JSX files
  ],
  theme: {
    extend: {
      // Add any custom colors, fonts, or other theme extensions here if needed
    },
  },
  plugins: [],
};
